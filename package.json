{
  "name": "mi-proyecto",
  "version": "1.0.0",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev:frontend",
    "dev:backend": "npm --prefix backend run dev:backend",
    "dev": "npm-run-all --parallel dev:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
